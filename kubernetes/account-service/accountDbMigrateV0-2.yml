apiVersion: batch/v1
kind: Job
metadata:
  name: account-db-migrate-v0-2
spec:
  backoffLimit: 3
  template:
    metadata:
      labels:
        app: account-db-migrate
    spec:
      restartPolicy: Never
      containers:
        - name: migrate
          image: saujandsre/account-service:v0.2
          command: ["alembic", "upgrade", "head"]
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: account-db-secret
                  key: DATABASE_URL
